kind: GitRepo
apiVersion: fleet.cattle.io/v1alpha1
metadata:
  name: cluster-templates
  namespace: fleet-default
spec:
  repo: https://github.com/bcdurden/fleet-demo.git
  branch: main  
  targets:
  - clusterName: local

  paths:
  - "./gitops/clusters/test/helm"